PORT=3000
HOST=127.0.0.1
# Local development defaults to database shiftlog and your OS PostgreSQL user.
# DATABASE_URL=postgresql://username:password@localhost:5432/shiftlog
# PGSSLMODE=require
# Only for the isolated integration test database (never your personal database):
# TEST_DATABASE_URL=postgresql://localhost/shiftlog_test
